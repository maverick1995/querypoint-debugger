This script is used to build and upload a CRX file using the latest releases of devtoolsExtended, devtools-save and QuerypointPrototype.

The script automatically increases the build's version number. In the file 'version' there's a number indicating the latest release.

To use the script execute release.sh providing the private key and google-code user name. Enter password when requested.
